# yaml-language-server: $schema=.schema/talconfig.json

.variables:
  cluster_name: &cname homelab
  all_node_patches:
    # https://kubernetes.io/docs/tutorials/security/seccomp/#enable-the-use-of-runtimedefault-as-the-default-seccomp-profile-for-all-workloads
    # Talos machine config patch to enable default seccomp
    - &SEC_COMP_RUNTIME_DEFAULT |-
      machine:
        kubelet:
          defaultRuntimeSeccompProfileEnabled: true


clusterName: *cname
# https://github.com/siderolabs/talos/releases
talosVersion: v1.13.6
# https://kubernetes.io/releases/
kubernetesVersion: v1.36.2
endpoint: https://${control_plane_ip}:6443
allowSchedulingOnMasters: true
additionalApiServerCertSans:
  - homelab.local
  - home.pontifex.dev
clusterPodNets:
  - 10.244.0.0/16
clusterSvcNets:
  - 10.96.0.0/12
cniConfig:
  # Will install Cilium CNI
  name: none
nodes:
  - hostname: homelab-control-01
    installDisk: /dev/vda
    ipAddress: ${control_plane_ip}
    controlPlane: true

worker:
  nodeLabels:
      app.kubernetes.io/cluster: *cname
      topology.kubernetes.io/region: home
      topology.kubernetes.io/zone: home-a
  patches:
    - *SEC_COMP_RUNTIME_DEFAULT

controlPlane:
  certSANs:
    - ${control_plane_ip}
  nodeLabels:
      app.kubernetes.io/cluster: *cname
      topology.kubernetes.io/region: home
      topology.kubernetes.io/zone: home-a
  patches:
    - *SEC_COMP_RUNTIME_DEFAULT
    # https://kubernetes.io/docs/tutorials/security/cluster-level-pss
    # https://kubernetes.io/docs/concepts/security/pod-security-standards/
    - |-
      cluster:
        apiServer:
          admissionControl:
            - name: PodSecurity
              configuration:
                apiVersion: pod-security.admission.config.k8s.io/v1
                kind: PodSecurityConfiguration
                defaults:
                  enforce: baseline
                  enforce-version: latest
                  warn: restricted
                  warn-version: latest
                  audit: restricted
                  audit-version: latest
                exemptions:
                  namespaces:
                    - kube-system
                  runtimeClasses: []
                  usernames: []
patches:
  # Replace kube-proxy with Cilium
  - |-
    cluster:
      proxy:
        disabled: true
    machine:
      sysctls:
        net.ipv4.ip_forward: "1"
        net.ipv6.conf.all.forwarding: "1"
        net.ipv4.conf.all.rp_filter: "0"
        net.ipv4.conf.default.rp_filter: "0"

